/** @deprecated Import from `../content/projects` in new code. */
export type { Project, ProjectFilterBucket } from '../content/projects';
export { projects, projectFilterBucket } from '../content/projects';
